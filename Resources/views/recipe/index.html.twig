{% extends 'base.html.twig' %}

{% block menu %}
    <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Strona główna</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('recipe_new') }}">Nowy przepis</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('security_logout') }}">Wyloguj się</a>
            </li>
        </ul>
    </div>
{% endblock %}

{% block header %}
    <h1>Książka kucharska</h1>
{% endblock %}

{% block body %}
    {#<table>
               <thead>
                   <tr>
                       <th>Title</th>
                       <th>Content</th>
                       <th>Actions</th>
                   </tr>
               </thead>
               <tbody>
               {% for recipe in recipes %}
                   <tr>
                       <td>{{ recipe.title }}</td>
                       <td>{{ recipe.content }}</td>
                       <td>
                           <ul>
                               <li>
                                   <a href="{{ path('recipe_show', { 'id': recipe.id }) }}">show</a>
                               </li>
                               <li>
                                   <a href="{{ path('recipe_edit', { 'id': recipe.id }) }}">edit</a>
                               </li>
                           </ul>
                       </td>
                   </tr>
               {% endfor %}
               </tbody>
           </table>#}


    <div class="row">
        <div class="col-lg-8 col-md-10 mx-auto">
            {% for recipe in recipes %}
                <div class="post-preview">
                    <a href="{{ path('recipe_show', { 'id': recipe.id }) }}">
                        <h2 class="post-title">
                            {{ recipe.title }}
                        </h2>
                    </a>
                    <p class="post-meta">tu może jakiś autor i data</p>
                </div>
                <hr>
            {% endfor %}
            <hr>
            <!-- Pager -->
            <div class="clearfix">
                <a class="btn btn-primary float-right" href="#">Dalej →</a>
            </div>
        </div>
    </div>
{% endblock %}
