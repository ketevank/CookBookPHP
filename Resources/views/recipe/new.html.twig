{% extends 'base.html.twig' %}

{% block body %}
    <h1>Nowy przepis</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('recipe_index') }}">Powrót do listy</a>
        </li>
    </ul>
{% endblock %}
