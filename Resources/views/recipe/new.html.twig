{% extends 'base.html.twig' %}

{% block menu %}
    <div class="collapse navbar-collapse" id="navbarResponsive">
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a class="nav-link" href="/">Strona główna</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('recipe_new') }}">Nowy przepis</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('security_logout') }}">Wyloguj się</a>
            </li>
        </ul>
    </div>
{% endblock %}

{% block header %}
    <h1>Książka kucharska</h1>
{% endblock %}

{% block body %}
    <h1>Nowy przepis</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <a href="{{ path('ingredient_new') }}">Dodaj składnik</a>
        <input type="submit" value="Zapisz" />
    {{ form_end(form) }}

    <a href="{{ path('recipe_index') }}">Powrót do listy</a>
{% endblock %}
